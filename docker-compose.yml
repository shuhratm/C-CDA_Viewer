version: '3.8'

services:
  ccda-viewer:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - "${RECORDS_PATH:-./medical-records}:/app/medical-records:ro"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    container_name: personal-ccda-viewer